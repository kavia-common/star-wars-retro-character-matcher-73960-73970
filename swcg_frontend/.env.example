# Frontend environment configuration for SWCG
# IMPORTANT: Do not commit real secrets. Copy this to .env and set values.

# Base URL for the backend FastAPI server (no trailing slash)
REACT_APP_API_BASE_URL=http://localhost:3001

# Public site URL used when building share links (no trailing slash)
REACT_APP_SITE_URL=http://localhost:3000
